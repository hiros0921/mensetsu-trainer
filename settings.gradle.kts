rootProject.name = "mendan-training"

// モジュールを2つに分ける。
//
// 【重要】これは好みの問題ではない。仕様書8章①「ドメイン層をフレームワークから
// 独立させること」を、規約ではなく構成で保証するための分割。
//
// 同じモジュールの中でパッケージだけ分けると、domain のクラスからでも
// import org.springframework... と書けてしまう。書ければ、いつか書かれる。
// モジュールを分けて domain の依存に Spring を入れなければ、
// 書いた瞬間にコンパイルが通らない。「気をつける」ではなく「できない」にする。
include("domain")
include("app")
